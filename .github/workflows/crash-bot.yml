name: Crash Bot Monitor

on:
  schedule:
    - cron: '*/10 * * * *'   # كل 10 دقايق – غيّره لـ */5 لو عايز أسرع
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 12

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies & Playwright
      run: |
        python -m pip install --upgrade pip
        pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
        pip install -r requirements.txt
        playwright install-deps
        playwright install chromium --with-deps

    - name: Run Crash Bot
      run: python main.py

    - name: Commit updated files if changed
      run: |
        git config --global user.name "GitHub Action Bot"
        git config --global user.email "actions@github.com"
        git add crash_odds_PRO.csv debug_screenshot.png || true
        git commit -m "Update odds & debug screenshot [auto]" || echo "No changes"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
